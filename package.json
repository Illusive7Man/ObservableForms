{
  "author": {
    "email": "illusive7man@gmail.com",
    "url": "https://github.com/Illusive7Man",
    "name": "unknown"
  },
  "bugs": {
    "url": "add later__"
  },
  "name": "observable-forms",
  "dependencies": {
    "@popperjs/core": "^2.5.3",
    "@types/jquery": "^3.5.5",
    "rxjs": "^6.6.3"
  },
  "description": "Adds observable streams to jQuery objects of forms and form inputs. Adds properties such as valueChanges and statusChanges (inspired by Angular) to $('#some-input').",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^9.0.0",
    "@rollup/plugin-typescript": "^6.1.0",
    "rollup": "^2.28.1",
    "rollup-plugin-sourcemaps": "^0.6.2",
    "rollup-plugin-terser": "^7.0.2",
    "tslib": "^2.0.1",
    "typescript": "^3.9.7"
  },
  "homepage": "add later__",
  "keywords": [
    "jQuery",
    "form",
    "angular",
    "observable",
    "reactive"
  ],
  "license": "Apache-2.0",
  "main": "dist/index.min.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adobe/spectrum-web-components.git",
    "directory": "packages/circle-loader"
  },
  "scripts": {
    "clean": "if exist dist rmdir /s /q dist",
    "build": "npm run clean && tsc --project tsconfig.json && npm run bundle:esm && npm run bundle:esm:min",
    "bundle:esm": "rollup -c",
    "bundle:esm:min": "terser --ecma 6 --compress --mangle --module -o dist/index.min.js -- dist/index.js"
  },
  "type": "module",
  "version": "0.9.0"
}
